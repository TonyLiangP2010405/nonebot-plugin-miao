# 对接文档：nonebot-plugin-miao 移植项目交接

> 写给下一位接手开发的 AI agent。你之前没有接触过本项目，请完整阅读本文档后再动手。
> 本文档最后更新于 P6 阶段完成时（pytest 179 passed，ruff 全绿）。

## 一、项目是什么

把 Yunzai-Bot 生态的 JS 插件 **miao-plugin**（原神/星铁的抽卡分析、模拟抽卡、角色面板）移植成一个独立的 **NoneBot2 Python 插件**。

- 项目路径：`/Users/liangpuyue/Desktop/develop/nonebot-plugin-miao`
- 包名 `nonebot-plugin-miao`，模块名 `nonebot_plugin_miao`，OneBot V11 适配器，Poetry 管理，MIT，GitHub 目标仓库 `TonyLiangP2010405/nonebot-plugin-miao`
- 参考源码（本地已克隆，只读）：
  - `/Users/liangpuyue/Desktop/develop/refs/miao-plugin` —— 面板/抽卡分析/渲染模板的原版（MIT）
  - `/Users/liangpuyue/Desktop/develop/refs/Yunzai-genshin` —— 抽卡记录抓取（gachaLog.js）与十连模拟抽卡（model/gachaData.js + defSet/gacha/*.yaml）的原版
  - `/Users/liangpuyue/Desktop/develop/nonebot-plugin-bili-dynamic` —— 同作者的 NoneBot 插件，参考其图片发送模式（tempfile + `MessageSegment.image("file:///")` + 延迟删除）、测试模式
  - 米游社 DS 签名源码在 `/tmp/Miao-Yunzai`（可能已不存在；重新克隆用镜像：`git clone --depth 1 https://gh-proxy.com/https://github.com/yoimiya-kokomi/Miao-Yunzai.git`，**本机直连 github.com 不通，必须用 gh-proxy.com 镜像**；jsdelivr CDN 可直连）

## 二、用户已拍板的决策（不要推翻）

1. 游戏范围：原神 + 星铁
2. 渲染：**skia-python 手绘**（不用 HTML/playwright；用户明知 bili-dynamic 是 skia 手绘仍选此方案）
3. 伤害计算：**全量移植**（150 原神角色 + 106 星铁角色的 calc 规则 + 通用引擎）
4. 面板数据源：enka.network（原神）+ mihomo（星铁）+ 米游社（DS 签名 + cookie，已完成）
5. 完成后推送 GitHub + 发布 PyPI
6. 除模拟抽卡外，所有功能都必须以 `/` 开头；模拟抽卡可直接发送 `十连`，并兼容 `#十连`、`/十连`

## 三、总体进度

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 | 脚手架、pyproject、资源同步脚本、CI | ✅ |
| P1 | config / store（绑定/CD/文件存储）/ meta 加载器 | ✅ |
| P2 | 抽卡抓取（authkey 解析、getGachaLog 增量分页、UIGF 导入导出） | ✅ |
| P3 | 抽卡分析 analyse/stat（node 交叉验证 19/19 一致） | ✅ |
| P3b | 十连模拟抽卡（保底/定轨/每日限制） | ✅ |
| P4 | skia 渲染基础设施 + 3 张抽卡卡片 + 抽卡/绑定命令层 | ✅ |
| P5 | enka/mihomo 抓取解析、属性计算、圣遗物评分（交叉验证 1e-9 精度）、面板文本命令 | ✅ |
| P6 | 米游社数据源（DS 签名、character detail/avatarInfo 映射） | ✅ |
| **P7** | **伤害计算（quickjs 内嵌执行原版规则）** | ⬜ 下一个任务 |
| **P8** | **面板渲染（profile_list/profile_detail/artis_list 卡片）** | ⬜ |
| **P9** | **收尾：帮助、README、全量测试** | ⬜ |
| **P10** | **发布：git init、GitHub、PyPI** | ⬜ |

## 四、开发约定（每个阶段都必须遵守）

- 所有命令在项目根目录用 `poetry run` 执行（环境已装好：nonebot2 / httpx / skia-python 144 / quickjs 1.19.4 / nonebot-plugin-localstore / respx(dev) / pytest / ruff）
- **每阶段关口（全部真实通过，禁止编造输出）**：
  ```
  poetry run python -m compileall nonebot_plugin_miao
  poetry run pytest -v
  poetry run ruff check .
  ```
- ruff line-length 120，target py39（pyproject 里实际 python 约束是 `>=3.9,<4.0`，有 numpy 分档约束，勿动）
- 中文注释；字段名对齐 JS 原版用 camelCase；**不要改动已有模块的公开接口**（后续阶段都建立在这些接口上）
- tests/conftest.py 已配置 nonebug init（`driver="~none"`）；store 测试用 `monkeypatch` 重定向 `store._data_dir` 到 tmp_path，别污染真实数据目录
- 异步网络请求一律 httpx.AsyncClient，禁止同步阻塞
- 配置项全部有默认值，零配置可导入；cookie 等敏感信息只走 store/config，不写死
- **P10 之前不做任何 git 操作**
- 遇到错误输入（空参数/无绑定/无记录/CD 中/API 失败）必须有中文提示，不能把 traceback 发给用户——用 `commands/common.py` 的 `guard` 装饰器

## 五、已有模块接口速查（按字母序，都在 `nonebot_plugin_miao/` 下）

### commands/（命令层，已在 `__init__.py` 注册 bind/gacha/profile）
- `common.py`：`send_image(matcher, png_bytes)`、`resolve_uid(event, args_text, game)`（消息内 UID 正则 `(18|[1-9])\d{8,9}` 优先 → 绑定兜底）、`is_sr(text)`、`game_of(text)`、`guard(matcher)` 异常包装
- `bind.py`：`/绑定uid` / `/星铁绑定uid` / `/删除(星铁)绑定` / `/绑定cookie`（仅私聊）/ `/删除cookie` / `/我的绑定`
- `gacha.py`：authkey 嗅探（消息以 `/` 开头且含 `authkey=`，priority 15）/ `/(星铁)更新抽卡记录` / 记录分析 `^/(星铁)?(抽卡|抽奖|角色|武器|光锥|常驻|集录|up)+池?(记录|祈愿|分析)$` / 统计 `...池?统计$` / 模拟 `十连|十连2|武器十连|常驻十连|单抽`（唯一无需 `/` 的功能，兼容 `#`、`/`）/ `/定轨` / `/(星铁)导入记录|/导出记录`
- `profile.py`：`/(星铁)更新面板` / `/面板列表` / `/(星铁)(圣遗物|遗器)列表` / `/角色名(面板|详情|圣遗物|遗器)`（纯文本输出，P8 换成图片）/ `/(星铁)米游社更新面板`

### core/
- `store.py`：`bind_uid/get_uid/del_bind(user_id, game)`（game ∈ {"gs","sr"}）、`set_cookie/get_cookie/del_cookie`、`check_cd(key, seconds)->剩余秒` / `set_cd`、`gacha_log_path/read_gacha_log/write_gacha_log(user_id, uid, gacha_type, game)`、`player_path/read_player/write_player(game, uid)`、`read_sim_state/write_sim_state(scope_key)`、`load_json/save_json`（原子写）。localstore 必须延迟导入（见"坑"）
- `meta.py`：`get_character(name_or_alias, game) -> CharacterMeta`（`__getattr__` 代理字段，支持 name/abbr/id/别名）、`get_weapon(name, game)` / `get_weapon_by_id(id, game)`、`get_artifact_meta(game)`、`artifact_extra(game)`（gs=extra.js，sr=meta.js，含 attrMap/mainAttr/subAttr/attrPct）、`pool_info(game)` / `pool_data(game)`、`gacha_sim_config()`、`char_img(name, kind, game)` / `weapon_img(...)`（返回相对 resources 路径，文件不一定存在）、`_eval_esm(path, export_names)`（quickjs 求值无 import 的 ESM 元数据文件）
- `player.py`：`Player`（load/save/update/get_avatar(别名解析)/avatar_names）；avatar 统一结构见 P5a 总结（`id/name/elem/level/promote/cons/fetter/costume/talent{a,e,q(,t,z,me,mt,xe)}/trees(sr)/weapon{id,name,level,promote,affix}/artis{"1".."5|6":{id,level,star,mainId,attrIds}}/_source/_time`，sr 的 attrIds 元素是 `"affixId,cnt,step"` 字符串）
- `attr_calc.py`：`calc_attr(avatar, game) -> dict`（各属性 computed + `hpBase/atkBase/defBase` + `staticAttr{key:{base,plus,pct}}` + `_base` + `_calc`）、`resolve_artis(avatar, game)`、`calc_promote`、`is_elem/same_elem/elem_name`
- `artis_mark.py`：`get_char_weight(name, attr, game, ...)`（artis.js 经 quickjs 执行，含武器/套装修正）、`calc_mark(avatar, attr, game) -> {classTitle, artis{idx:{name,set,level,main,attrs,mark,markClass,...}}, mark, markClass, sets, names, charWeight}`（档位 ≥70 返回 None）

### datasource/
- `gacha_log.py`：`GachaLogError`、`parse_authkey_url(text)`、`fetch_gacha_log(authkey_info, gacha_type)`、`update_gacha_log(user_id, uid, authkey_info, pool_types=None) -> {type: 新增}`、`save_authkey/load_authkey`（24h）、`GACHA_TYPES/DEFAULT_POOLS/is_cn_region/region_from_uid`
- `uigf.py`：`import_uigf(user_id, uigf_dict)`、`export_uigf(user_id, uid, game)`
- `errors.py`：`ProfileError(message, source)`
- `enka.py`：`fetch_enka(uid)` / `parse_enka(raw, uid)`；`mihomo.py`：`fetch_mihomo/parse_mihomo`
- `profile_service.py`：`update_profile(user_id, uid, game) -> {code:"ok"|"cd", ...}`（CD key `profile:{game}:{uid}`，取 max(配置, ttl)）
- `mys.py`：`get_ds(query, body, salt, with_b, t=None, r=None)`（salt 国服 `xV8v4Qu54lUKrEYFZkJhB8cuOh9Asafs`，出处 Miao-Yunzai mysApi.js:201-212）、`MysApi(cookie, game)`（`get_character_ids/gs_panel/sr_panel`）、`parse_gs_panel/parse_sr_panel`、`update_profile_mys(user_id, uid, game) -> {code:"ok"|"cd"|"no_cookie", ...}`（CD key `profile:mys:{game}:{uid}`；cookie 优先级 用户绑定 > 全局配置；仅支持国服 UID）

### gacha/
- `analyse.py`：`analyse_pool(user_id, uid, keyword, game)` / `stat_pool(...)`（无记录返回 None）、`pool_type_of(keyword, game)`
- `simulate.py`：`do_gacha(scope_key, kind, is_master, daily_limit, rng=None) -> {code:"ok"|"limit", msg, list(10项), info, nowFive, nowFour, poolName, isWeapon, bingWeapon, lifeNum}`、`toggle_bing(scope_key) -> str`；scope_key 群 `"{group_id}:{user_id}"` 私 `"private:{user_id}"`

### render/（skia，全部 async -> PNG bytes）
- `base.py`：`font(size, kind)` / `num_font`、`elem_gradient(elem)`、`fetch_image(rel_path)`（包内资源 > 磁盘缓存 > `{config.miao_res_mirror}/{rel_path}` 下载，失败 None）、`get_local_image`、`draw_image_or_placeholder`、`render_card(width, builder)`（两遍法测高）、圆角/阴影/渐变/星星/省略号助手
- `gacha_detail.py`：`render_gacha_detail(data, uid, game, pool_label, face=None)`
- `gacha_stat.py`：`render_gacha_stat(data, uid, game)`
- `gacha_trial.py`：`render_gacha_trial(result, sender_name)`
- demo：`poetry run python tools/render_demo.py` → `outputs/demo/*.png`

### resources/（已打包进 wheel，`tools/sync_resources.py` 重新同步）
`meta-gs/`、`meta-sr/`（990 个文本元数据：data.json/calc.js/artis.js/alias.js/extra.js/pool 等，**不含图片**）、`fonts/`（4 字体）、`imgs/`（占位图）、`gacha-sim/`（卡池 YAML 转的 JSON）

## 六、剩余阶段详细规格

### P7 伤害计算（最大的一块，先做）

目标：实现 `/角色名伤害` / `/角色名伤害N` 命令（如 `/刻晴伤害1`），全量移植伤害计算。

**原版代码**（`refs/miao-plugin/`）：
- 通用引擎 `models/dmg/`：DmgAttr.js(263) + DmgBuffs.js(109) + DmgCalc.js(402) + DmgCalcMeta.js(410) + DmgMastery.js(27)
- 编排 `models/ProfileDmg.js`(296)：加载角色 calc 规则、组织 buffs、敌人等级/抗性（`#敌人等级` 用户配置可不做，用默认值）
- 角色规则 `resources/meta-{gs,sr}/character/{名}/calc.js`（还有 calc_auto.js）：约 1.7 万行，**无 import 的 ESM**，导出 `details[]`（每项 {title, params, dmg: ({attr,talent,cons}, dmg) => {...}}）、`buffs[]`（{title, cons, data} 或字符串预设名如 'aggravate'）、`mainAttr`、`defDmgIdx` 等
- 武器/圣遗物的 calc.js 特效 buff 也参与伤害（读 ProfileDmg.js 确认加载链路）

**既定技术路线（不要改）**：不逐行翻译规则。用 `quickjs` Python 包内嵌执行原版 JS：
1. 写 `dmg/js_runtime.py`：quickjs.Context 沙箱；加载 calc.js 时用正则剥离 `export ` 关键字后 eval（`_eval_esm` 已有类似手法可参考，但这里要保留函数，不能 JSON 化——eval 后把 details/buffs 留在 JS 侧）
2. 把通用引擎（DmgAttr/DmgBuffs/DmgCalc/DmgCalcMeta/DmgMastery）改编成一份无 import 的 `dmg/js/dmg_runtime.js`（它们本身是 JS，主要是剥离 import、把对 Meta/Character 的依赖改为注入的全局数据对象）。Python 侧把 calc_attr 产物、avatar、meta（角色 data.json 的 talent 倍率表、武器、圣遗物套装）作为 JSON 注入，调 JS 函数算出每段伤害 {dmg, avg}，JSON 返回
3. Python 侧 `dmg/service.py`：`calc_dmg(avatar, game, idx=None) -> dict`（对齐 ProfileDmg.js 的编排：buff 排序、cons 过滤、反应计算；产物结构对齐原版 profile-detail 模板里的 `dmgCalc{dmgMsg, dmgData, dmgRet}`，P8 渲染直接消费）
4. 命令：profile.py 的 RE_DETAIL 已含 `伤害([1-9]+\d*)?` 之类关键词（读现有正则确认），伤害模式调 calc_dmg，先纯文本输出（P8 换图）
5. **交叉验证**（参照 tools/verify_gacha.mjs、tools/verify_attr.mjs 的成熟手法：node 里 `new Function` + 最小 stub 加载真源码）：tools/verify_dmg.mjs 跑原版引擎 + Python 跑 quickjs 版，至少 3 个角色（建议 刻晴[激化]、那维莱特[直伤]、一个星铁角色）逐段伤害比对，误差 <0.5%
6. 测试：tests/test_dmg.py，含反应（激化/蒸发）、命座过滤、defDmgIdx 默认条目、伤害N 选段

难点提示：dmg 回调 `dmg(pct, mode, reaction)` 是引擎传给规则的函数——把规则和引擎放同一个 JS 沙箱里，这个边界就消失；Python 只进不出（注入数据、取结果）。quickjs 内存上限注意调大（规则文件大）。

### P8 面板渲染（skia）

目标：把 profile.py 的文本输出换成图片卡片。渲染模式照 render/gacha_*.py 的既有模式（render_card 两遍法、fetch_image CDN、元素渐变）。

三张卡片（视觉对照 `refs/miao-plugin/resources/character/` 下的 profile-list.html、profile-detail.html、artis-mark.html / artis-list.html，读懂布局后手绘，神似即可）：
- `render/profile_list.py`：`render_profile_list(player, game) -> bytes`——角色网格（头像/名字/等级/命座/评分档位），头部玩家信息+数据源+更新时间
- `render/profile_detail.py`：`render_profile_detail(avatar, attr, mark, game, dmg=None) -> bytes`——最复杂的一张：左侧角色立绘（`char_img(name, "splash"/"banner", game)` 走 CDN，旅行者多元素目录注意）、右侧属性表（staticAttr 的 base+plus，绿字加成）、武器行（图标/等级/精炼）、圣遗物 5-6 件（图标/等级/主词条/评分/档位色）、总评分大字、天赋等级行；dmg 模式附伤害列表
- `render/artis_list.py`：`render_artis_list(player, game) -> bytes`——按角色分组的圣遗物简表
- 把 profile.py 的 `/面板列表`、`/角色名面板`、`/角色名圣遗物`、`/角色名伤害`、`/圣遗物列表` 输出换成 send_image
- 更新 tools/render_demo.py 加面板 demo，生成后**必须肉眼检查 PNG**（ReadMediaFile）
- 测试：monkeypatch fetch_image 为 None 的占位分支，断言 PNG 合法

### P9 收尾

- `/面板帮助` / `/抽卡帮助`（文本版即可，列出全部命令）
- README.md 写完整：功能表、安装（nb plugin install / pip / poetry）、配置表（config.py 全部 4-6 项）、命令表（指令/权限/范围/说明）、**移植对照表**（原 miao-plugin 功能 → 本插件实现位置 → 是否完成）、明确不移植的功能（面板替换 #刻晴换雾切、群内排名、练度统计、面板图上传、miao 私有 API、mgg/hutao/avocado 备选源、Yunzai 按钮）、致谢与 LICENSE 说明（miao-plugin MIT）
- 全量 pytest + ruff + compileall 最终关口
- 检查 `__init__.py` 的 PluginMetadata（name/description/usage/homepage/config/supported_adapters）信息完整准确

### P10 发布

- `git init && git add . && git commit -m "feat: initial release"`（注意 .gitignore 已含 outputs/、dist/ 等）
- `gh repo create TonyLiangP2010405/nonebot-plugin-miao --public --source=. --remote=origin --push`（gh 已登录的话直接可用；没有权限就如实告诉用户，给出命令，**禁止假装推送成功**）
- 打 tag：`git tag v0.1.0 && git push --tags`（.github/workflows/pypi-publish.yml 已就绪，tag 触发 PyPI 发布，需要用户在 GitHub 仓库 Settings 配 `PYPI_API_TOKEN` secret——提醒用户）
- 本地发布备选：`poetry build && poetry publish`（需要 `poetry config pypi-token.pypi <token>`；没有 token 就如实报告）
- 发布后验证：`pip install nonebot-plugin-miao` 到新环境，`nonebot.init(); nonebot.load_plugin("nonebot_plugin_miao")` 能加载

## 七、交叉验证方法论（本项目质量基石，新阶段沿用）

对从 JS 移植的计算逻辑，用 node 跑原版代码产出基准、Python 输出逐字段 diff：
- 手法：node 脚本读原版源码 → 正则剥离 import/export → `new Function` 注入最小 stub（lodash 只实现用到的函数、Format.pct/comma、Meta 数据从 refs/miao-plugin/resources 真加载）→ eval 后调用
- 现成范例：`tools/verify_gacha.mjs`（抽卡分析，19/19 一致）、`tools/verify_attr.mjs`（属性+评分，1e-9 精度；配 verify_attr_py.py 和 tools/attr_cases.json）
- fixture：tests/fixtures/ 有真实录制的 enka_800055548.json、mihomo_702762444.json；tools/fixtures/gacha/ 有手工抽卡记录

## 八、已知坑（前人踩过，别再踩）

1. **本机直连 github.com 不通**，git clone/raw 走 `https://gh-proxy.com/https://github.com/...`；jsdelivr 可直连
2. **nonebug 0.4.4** 有 session 级 autouse fixture 抢先 init——conftest.py 已用 `NONEBOT_INIT_KWARGS` stash 解决，别改回手动 init
3. **nonebot_plugin_localstore 顶层 import 会要求 nonebot 已初始化**——store/meta 里必须函数内延迟导入；demo 脚本里 localstore 不可用，fetch_image 已做内存缓存降级
4. **test_load.py 在加载前从 sys.modules 弹出 `nonebot_plugin_miao*`**（其他测试模块先 import 了包会报 "Make sure not to import it before loading"），保留这个写法
5. **skia-python 144 枚举改名**：`skia.BlurStyle.kNormal_BlurStyle`（不是 kNormal）；woff 字体本机可加载
6. **JS 的 toFixed 是四舍五入**，Python round 是银行家舍入——抽卡分析里用 `Decimal + ROUND_HALF_UP` 复刻（见 analyse.py）
7. **JS 对象整数 key 按数值升序迭代**（与插入顺序无关），影响 maxFour 平票和 stat items 排序（见 analyse.py 的 `_id_order`）
8. miao-plugin 的"新版本"兜底区间 end 硬编码 2025-12-31（已过期，新卡池记录会落入"未知"全记为歪）——照抄了原版行为，如需修要同时改 pool 数据
9. sr 武器/角色 id 在 meta 里是字符串，gs 圣遗物 id 第 4 位经 `[4,2,5,1,3]` 映射部位；sr 角色 elem 是中文、gs 是英文（attr_calc 已处理双向映射）
10. miao meta 武器字段名不是文档里的 bonusKey/bonusData，实际是 affixTitle/affixData 等（dict 透传，以实际文件为准）
11. 直接 `python` 导入包时 gacha.py 有 ForwardRef("Bot") 警告（pytest 下不出现），已知噪音未修，P9 可顺手处理
12. 旅行者/开拓者多元素图片目录（`旅行者/{elem}/`）未特判，P8 渲染时注意

## 九、原始计划存档

完整计划（含背景调研结论）在：
`/Users/liangpuyue/.kimi-code/sessions/wd_develop_158e1d132dcc/session_7d5ce916-d3fc-4a83-93c4-e05700c32a08/agents/main/plans/mantis-wiccan-metamorpho.md`
（若该路径失效，以本文档为准。）

## 十、上手第一步

```
cd /Users/liangpuyue/Desktop/develop/nonebot-plugin-miao
poetry run pytest -q    # 应看到 179 passed
poetry run ruff check . # All checks passed
ls refs 2>/dev/null; ls /Users/liangpuyue/Desktop/develop/refs  # 确认参考源码在
```
然后开始 P7。祝顺利。
